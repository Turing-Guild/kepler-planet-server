{
  "name": "kepler-planet-server",
  "version": "0.1.0",
  "description": "Hello World planet simulation server for Kepler-442b habitat channels.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "npm run build && node dist/index.js",
    "test": "node --test test/*.test.js"
  },
  "keywords": [
    "websocket",
    "simulation",
    "kepler-442b",
    "habitat"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "marked": "^18.0.5",
    "ws": "^8.18.2"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "@types/ws": "^8.18.1",
    "typescript": "^5.9.2"
  }
}
